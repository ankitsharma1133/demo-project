node('jenkins-slave') {
    
     stage('unit-tests') {
        sh(script: """
            echo 'Hello world! on cli ' 
           sudo docker run --rm alpine /bin/sh -c "echo hello world"
        """)
    }
}
